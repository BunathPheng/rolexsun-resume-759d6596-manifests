---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: rolexsun-resume-759d6596                  # Ingress name
  labels:
    app: rolexsun-resume-759d6596
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "rolexsun-resume-759d6596.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: rolexsun-resume-759d6596               # Links to Service name dynamically
                port:
                  number: 3000  # Links to service port dynamically
